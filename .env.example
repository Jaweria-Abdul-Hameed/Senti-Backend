# Copy this file to .env and fill in real values. Load it with `python-dotenv`
# or export these in your shell before running uvicorn.

# Groq is the primary/only model actually used right now -- see ai/groq_provider.py
# and ai/hybrid_provider.py, both read os.getenv("GROQ_API_KEY") / os.getenv("GROQ_MODEL").
# Get a key at https://console.groq.com/keys
GROQ_API_KEY=your-real-groq-api-key-here
GROQ_MODEL=llama-3.3-70b-versatile

# Kept for reference only -- searched the whole codebase, GEMINI_API_KEY is not read
# anywhere anymore (ai_setup.py and routers/tiles.py only mention it in comments).
# Safe to delete these two lines whenever; leaving them costs nothing.
GEMINI_API_KEY=your-real-gemini-api-key-here
GEMINI_MODEL=gemini-3.5-flash

# Postgres connection. IMPORTANT for Vercel: this is currently the DIRECT connection
# (port 5432), which is IPv6-only on Supabase unless you've bought the IPv4 add-on --
# Vercel's serverless functions can't reach it and you'll get connection failures/
# timeouts in prod even though this works fine from your own machine.
# Use the Transaction Pooler string instead: Supabase dashboard -> Connect ->
# "Transaction pooler" tab -> copy that URI (port 6543, host is
# aws-0-<region>.pooler.supabase.com, username becomes postgres.<project-ref>).
# It looks like:
# postgresql://postgres.ojxpsfpvepcluvuriuok:Ufz4OGA8IrzGbWpA@aws-0-<region>.pooler.supabase.com:6543/postgres?sslmode=require
DATABASE_URL=

SQL_ECHO=false

# Signs/verifies JWT access + password-reset tokens (security.py). Generate
# a real one with: python -c "import secrets; print(secrets.token_urlsafe(48))"
# If unset, a random one is generated per-process (all tokens invalidated on restart).
JWT_SECRET_KEY=

# The WEB client ID (not Android client ID) from Google Cloud Console ->
# APIs & Services -> Credentials -> OAuth 2.0 Client IDs. Used both as the
# Android app's serverClientId when requesting a Google ID token, and here
# on the backend to verify that token was really issued for your app.
GOOGLE_CLIENT_ID=

# --- Nearby crisis resources map (routers/resources.py) ---
# Free API key, no credit card required: sign up at
# https://myprojects.geoapify.com, create a project, copy the key it gives
# you. Free tier is 3000 requests/day, plenty for dev + demo use.
GEOAPIFY_API_KEY=


# --- Password reset emails (email_service.py) ---
# Without these, forgot-password still "works" but the reset code only
# shows up in the server log, not an actual email.
#
# Easiest option: a Gmail account with an "App Password" (NOT your normal
# Gmail password — Google blocks that for SMTP). Create one at
# https://myaccount.google.com/apppasswords (requires 2-Step Verification
# to be enabled on the account first).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-gmail-address@gmail.com
SMTP_PASSWORD=your-16-char-app-password
SMTP_FROM_EMAIL=your-gmail-address@gmail.com
SMTP_USE_TLS=true